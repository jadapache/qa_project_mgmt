{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for default app windows",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
